package tools.crypto

import java.util.concurrent.locks.ReentrantLock

import tools.ByteTools

public class MapleAES {

	private AES cipher
	private ReentrantLock lock
	private static short sVersion, rVersion
	private static final int[] SHUFFLE_BYTES = []

	public MapleAES() {
		cipher = new AES()
		cipher.setKey([
			0x13, 0x00, 0x00, 0x00,
			0x08, 0x00, 0x00, 0x00,
			0x06, 0x00, 0x00, 0x00,
			(byte) 0xB4, 0x00, 0x00, 0x00,
			0x1B, 0x00, 0x00, 0x00,
			0x0F, 0x00, 0x00, 0x00,
			0x33, 0x00, 0x00, 0x00,
			0x52, 0x00, 0x00, 0x00
		] as byte[])
		lock = new ReentrantLock(true)
	}

	public static void initialize(short v) {
		sVersion = (short) ((((0xFFFF - v) >> 8) & 0xFF) | (((0xFFFF - v) << 8) & 0xFF00))
		rVersion = (short) (((v >> 8) & 0xFF) | ((v << 8) & 0xFF00))
	}

	public byte[] crypt(byte[] delta, byte[] gamma) {
		int a = delta.length
		int b = 0x5B0
		int c = 0
		while (a > 0) {
			byte[] d = ByteTools.multiplyBytes(gamma, 4, 4)
			if (a < b) {
				b = a
			}
			for (int e = c; e < (c + b); e++) {
				if ((e - c) % d.length == 0) {
					lock.lock()
					try {
						cipher.encrypt(d)
						// not sure if encrypt applies changes
						// to the actual pointer or creates a
						// new byte array on the stack
						//                        System.arraycopy(f, 0, d, 0, d.length); // not sure, but whatever...
					} catch (Exception ex) {
						ex.printStackTrace()
					} finally {
						lock.unlock()
					}
				}
				delta[e] ^= d[(e - c) % d.length]
			}
			c += b
			a -= b
			b = 0x5B4
		}
		return delta
	}

	public static byte[] getHeader(int delta, byte[] gamma) {
		int a = (gamma[3]) & 0xFF
		a |= (gamma[2] << 8) & 0xFF00
		a ^= sVersion
		int b = ((delta << 8) & 0xFF00) | (delta >>> 8)
		int c = a ^ b
		byte[] ret = new byte[4]
		ret[0] = (byte) ((a >>> 8) & 0xFF)
		ret[1] = (byte) (a & 0xFF)
		ret[2] = (byte) ((c >>> 8) & 0xFF)
		ret[3] = (byte) (c & 0xFF)
		return ret
	}

	public static int getLength(int delta) {
		int a = ((delta >>> 16) ^ (delta & 0xFFFF))
		a = ((a << 8) & 0xFF00) | ((a >>> 8) & 0xFF)
		return a
	}

	public static boolean checkPacket(byte[] delta, byte[] gamma) {
		return ((((delta[0] ^ gamma[2]) & 0xFF) == ((rVersion >> 8) & 0xFF))
		&& (((delta[1] ^ gamma[3]) & 0xFF) == (rVersion & 0xFF)))
	}

	public static boolean checkPacket(int delta, byte[] gamma) {
		byte[] a = new byte[2]
		a[0] = (byte) ((delta >> 24) & 0xFF)
		a[1] = (byte) ((delta >> 16) & 0xFF)
		return checkPacket(a, gamma)
	}

	public static byte[] getNewIv(byte[] delta) {
		byte[] ret = delta
		int[] nIv = [
			0xF2, 0x53, 0x50, 0xC6
		]
		for (int i = 0; i < 4; i++) {
			int a = (ret[i] & 0xFF)
			int b = SHUFFLE_BYTES[a]
			nIv[0] += SHUFFLE_BYTES[nIv[1]] - a
			nIv[1] -= nIv[2] ^ b
			nIv[2] ^= SHUFFLE_BYTES[nIv[3]] + a
			nIv[3] -= nIv[0] - b
			int c = nIv[0] & 0xFF
			c |= (nIv[1] << 8) & 0xFF00
			c |= (nIv[2] << 16) & 0xFF0000
			c |= (nIv[3] << 24) & 0xFF000000
			int d = (c << 3) | (c >>> 0x1D)
			nIv[0] = (d & 0xFF)
			nIv[1] = ((d >> 8) & 0xFF)
			nIv[2] = ((d >> 16) & 0xFF)
			nIv[3] = ((d >> 24) & 0xFF)
		}
		for (int i = 0; i < 4; i++) {
			ret[i] = (byte) nIv[i]
		}
		nIv = null // explicitly nulling, because I don't want
		// this in the stack after I'm done
		return ret
	}
}
